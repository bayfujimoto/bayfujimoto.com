---
layout: layouts/archive.njk
title: Movies
description: Films I've watched, tracked via Letterboxd
permalink: /archive/movies/
---

<div class="movies-grid">
  {% for movie in movies %}
    <article class="movie-item">
      <a href="{{ movie.link }}" target="_blank" rel="noopener">
        {% if movie.image %}
          <img src="{{ movie.image }}" alt="{{ movie.title }}" loading="lazy">
        {% else %}
          <div class="no-image">{{ movie.title }}</div>
        {% endif %}
      </a>
      <div class="movie-info">
        <h3 class="movie-title">{{ movie.title }}</h3>
        <p class="movie-year">{{ movie.year }}</p>
        {% if movie.rating > 0 %}
          <p class="rating">
            {% for i in range(movie.rating) %}★{% endfor %}
          </p>
        {% endif %}
        <p class="movie-date">
          <time datetime="{{ movie.date }}">
            {{ movie.date | date }}
          </time>
        </p>
      </div>
    </article>
  {% endfor %}
</div>

{% if movies.length == 0 %}
  <p>No movies found. Check back later!</p>
{% endif %}
