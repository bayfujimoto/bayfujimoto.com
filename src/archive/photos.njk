---
layout: layouts/archive.njk
title: Photos
description: Photo gallery and photography collection
permalink: /archive/photos/
---

<div class="photos-gallery">
  <h1>Photo Gallery</h1>

  <p>A collection of photographs and visual memories.</p>

  <div class="photo-grid">
    {# Add your photos here #}
    {# Example:
    <div class="photo-item">
      <img src="/assets/images/photos/photo1.jpg" alt="Photo description" loading="lazy">
      <p class="photo-caption">Photo caption</p>
    </div>
    #}

    <p style="opacity: 0.5; text-align: center; margin-top: 2rem;">
      Photo gallery coming soon. Add photos to /assets/images/photos/ and update this template.
    </p>
  </div>
</div>

<style>
.photo-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: var(--spacing-medium);
  margin-top: var(--spacing-xl);
}

.photo-item {
  position: relative;
  overflow: hidden;
}

.photo-item img {
  width: 100%;
  height: auto;
  display: block;
  transition: transform 0.3s ease;
}

.photo-item:hover img {
  transform: scale(1.05);
}

.photo-caption {
  margin-top: var(--spacing-small);
  font-size: var(--font-size-small);
  text-align: center;
}

@media (max-width: 768px) {
  .photo-grid {
    grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
  }
}
</style>
