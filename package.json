{
  "name": "bayfujimoto-archive",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build-data.js && vite",
    "build": "node scripts/build-data.js && vite build",
    "build-data": "node scripts/build-data.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "glob": "^11.0.0",
    "vite": "^6.0.0"
  }
}
